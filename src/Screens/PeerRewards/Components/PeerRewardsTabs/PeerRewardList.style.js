import { StyleSheet } from 'react-native'
import { DefaultVariables } from '@/Theme'

export const peerRewardListStyle = StyleSheet.create({
  title: { fontSize: 15 },
  subtitle: {
    color: DefaultVariables.Colors.quickSilver,
  },
})
